# 运行数据绝不能进镜像
data/
*.db
*.db-shm
*.db-wal
config.yaml
.env

# 构建产物
# 一律带开头的斜杠，明确只指根目录那一个。
# 写成 leyun 容易让人以为连 cmd/leyun/ 也一起排掉了（.gitignore 里确实会），
# 加个斜杠把意图写死，省得下次有人照着改错。
/bin/
/dist/
/leyun
/leyun.exe
/internal/web/dist/assets/
/internal/web/dist/index.html

# 依赖与缓存
web/node_modules/
web/dist/
node_modules/

# 版本库与文档
.git/
.github/
.gitignore
*.md
!README.md
.idea/
.vscode/
